git:
  depth: 10

language: node_js

node_js:
  - "7"
  - "8"
  - "10"

cache:
  directories:
  - $HOME/.npm
  - node_modules

script:
  - npm run build:ci

after_script:
  - npm run test:publish

  # publishing to npm
  # uncomment when package will be ready to publish
  # if: tag IS present
  #   - npm run publish:ci
