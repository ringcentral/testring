/// <reference types="mocha" />

import * as chai from 'chai';

describe('get-cursor', () => {
    it('should get available position for cursor according to the current position', () => {

    });
});
